---
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
// import Reviews from "../components/Reviews.astro";
// import SideBySide from "../components/SideBySide.astro";
import Team from "../components/Team.astro";
---
<html>
    <head>
        <meta charset="utf-8" />
        <link rel="icon" type="image/svg+xml" href="/Inserir Transparente.svg" />
        <meta name="viewport" content="width=device-width", initial-scale=1.0"/>
        <meta name="generator" content={Astro.generator} />
        <title>Nosotros</title>
    </head>
  <body>
    <Navbar/>
    <div>
      <div class="foto-team">
          <img src="/l.jpg" alt="imgteam">
      </div>  
    </div>
    <div class="caja-text">
      <div class="text-nos">    
          <h1>Contamos con 2 directoras Médicas, especialistas en Medicina Física y Rehabilitación/ Fisiatras, quienes coordinan el equipo de profesionales. La Dra Rojo Graciela y la Dra Castello Rojo Fernanda</h1>
      </div>
    </div >

  
    <Team/>
    <Footer/>
    

  </body>
</html>





<style>
/* TODO ESTO PARA VERSION MOBILE O CELULAR */
@media only screen and (max-width: 768px) {
    .foto-team img {
        max-width: 100%; /* La imagen no excederá el ancho del contenedor */
        margin-top: 10px; /* Reduzco el margen superior para dispositivos móviles */
    }
    .caja-text {
        padding: 30px 0; /* Reduzco el padding superior e inferior para dispositivos móviles */
    }

    .text-nos {
      font-family: "Arial", sans-serif;
        font-size: 18px;
        color: black;
        text-align: center;
        max-width: 100%;
        margin: 0 auto;
        background-color: #f391c2;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .cs-topper {
        font-size: 1.2em; /* Reduzco el tamaño de la fuente del encabezado */
    }

    .cs-title {
        font-size: 1.5em; /* Reduzco el tamaño de la fuente del título */
    }

    .cs-text {
        font-size: 0.9em; /* Reduzco el tamaño de la fuente del texto */
    }
}


/* TODO ESTO PARA COMPUTADORA */

@media only screen and (min-width: 768px){
  .foto-team img {
    position: relative;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
    object-fit: cover;
    }

    .caja-text{
        display: flex;
        flex-direction: column; /* Cambia la dirección de flexión a vertical */
        align-items: center; /* Centra el contenido horizontalmente */
        padding: 50px 0;
    }

    .text-nos{
      font-family: 'Arial', sans-serif; /* Puedes cambiar la fuente a una que prefieras */
            font-size: 18px; /* Tamaño medio para el texto */
            color: black; /* Color de texto gris */
            text-align: center; /* Centrar el texto horizontalmente */
            max-width: 100%; /* Ancho máximo para el texto */
            margin: 0 auto; /* Centrar el texto horizontalmente */
            background-color: #f391c2; /* Color de fondo gris claro */
            padding: 20px; /* Espaciado interno */
            border-radius: 10px; /* Bordes redondeados */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Sombra suave */
    }
 :root {
    /* Add these styles to your global stylesheet, which is used across all site pages. You only need to do this once. All elements in the library derive their variables and base styles from this central sheet, simplifying site-wide edits. For instance, if you want to modify how your h2's appear across the site, you just update it once in the global styles, and the changes apply everywhere. */
    --primary: #ff6a3e;
    --primaryLight: #ffba43;
    --secondary: #ffba43;
    --secondaryLight: #ffba43;
    --headerColor: #1a1a1a;
    --bodyTextColor: #4e4b66;
    --bodyTextColorWhite: #fafbfc;
    /* 13px - 16px */
    --topperFontSize: clamp(0.8125rem, 1.6vw, 1rem);
    /* 31px - 49px */
    --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
    --bodyFontSize: 1rem;
    /* 60px - 100px top and bottom */
    --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
  }

  body {
    margin: 0;
    padding: 0;
    font-family:
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      Roboto,
      Oxygen,
      Ubuntu,
      Cantarell,
      "Open Sans",
      "Helvetica Neue",
      sans-serif;
  }

  *,
  *:before,
  *:after {
    /* prevents padding from affecting height and width */
    box-sizing: border-box;
  }
  .cs-topper {
    font-size: var(--topperFontSize);
    line-height: 1.2em;
    text-transform: uppercase;
    text-align: inherit;
    letter-spacing: 0.1em;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 0.25rem;
    display: block;
  }

  .cs-title {
    font-size: var(--headerFontSize);
    font-weight: 900;
    line-height: 1.2em;
    text-align: inherit;
    max-width: 43.75rem;
    margin: 0 0 1rem 0;
    color: var(--headerColor);
    position: relative;
  }

  .cs-text {
    font-size: var(--bodyFontSize);
    line-height: 1.5em;
    text-align: inherit;
    width: 100%;
    max-width: 40.625rem;
    margin: 0;
    color: var(--bodyTextColor);
  }

  
}

</style>